services:
  lo3-api:
    container_name: lo3-api
    restart: unless-stopped
    build:
      context: ./..
      dockerfile: ./lo3api/Dockerfile
    env_file:
       - '../.env'
    volumes:
      - ao3-db:/home/ao3db/
    ports:
      - "8080:8080"
volumes:
  ao3-db:
    external: true
